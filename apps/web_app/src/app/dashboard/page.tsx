'use client';

import Dashboard from '@/components/dashboard/Dashboard';

export default function DashboardPage() {
  // No changes needed for this file, it's already properly structured
  return <Dashboard />;
}
